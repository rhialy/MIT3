function doubleAll(numbers) {
    var newNumber = numbers * 2
    //numbers.map(doubleAll); warum?
    return newNumber;
}


//Nachdem nun Ihr Modul in From einer Funktion fertig ist, exportierne Sie es!
module.exports = doubleAll;